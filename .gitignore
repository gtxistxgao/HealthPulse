# The Xcode project is generated from project.yml (regenerate with
# `xcodegen generate`), but is committed so the build/acceptance environment —
# where the real xcodegen is unavailable — can build directly. Per-user state
# inside it is still ignored via xcuserdata/ below.

# macOS / Xcode cruft
.DS_Store
xcuserdata/
DerivedData/
